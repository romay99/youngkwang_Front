import MovieCard from "./MovieCard";
import styles from "./MovieCardList.module.css";

const MovieCardList = (props) => {
  return (
    <div>
      <div className={styles.moviePosterList}></div>
    </div>
  );
};

export default MovieCardList;
